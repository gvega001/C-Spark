@page "/about"
@using CSparkIDE.Shared.Services
@inject IFormFactor FormFactor

<PageTitle>About C-Spark IDE</PageTitle>

<style>
    /* Container for the page */
    .about-container {
        max-width: 900px;
        margin: auto;
        padding: 20px;
        font-family: Arial, sans-serif;
    }

    /* Enhanced Header section */
    .header-container {
        text-align: center;
        padding: 40px 20px;
        background: linear-gradient(135deg, #007acc, #61dafb);
        color: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        margin-bottom: 20px;
    }

        .header-container h1 {
            font-size: 2.8rem;
            margin: 0;
            font-weight: bold;
        }

        .header-container p {
            font-size: 1.4rem;
            margin-top: 10px;
        }

    /* Headings for cards */
    h2 {
        font-size: 1.8rem;
        margin-top: 20px;
    }

    /* Card-based layout */
    .card {
        background: #ffffff;
        border: 1px solid #ddd;
        border-radius: 10px;
        padding: 20px;
        margin-top: 15px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Code block styling */
    .code-block {
        font-family: Consolas, monospace;
        background: #f5f5f5;
        padding: 15px;
        border-radius: 8px;
        overflow-x: auto;
        border-left: 4px solid #007acc;
        margin-top: 10px;
    }

        .code-block h3 {
            margin-top: 0;
            color: #007acc;
        }
</style>

<div class="about-container">
    <!-- Page Header -->
    <div class="header-container">
        <h1>About C-Spark IDE</h1>
        <p>Write C-Spark code and transpile it to C instantly!</p>
    </div>

    <!-- Overview Section -->
    <div class="card">
        <h2>How C-Spark Works</h2>
        <p>
            C-Spark is a <strong>beginner-friendly programming language</strong> that simplifies coding while keeping the <strong>power of C</strong>.
            The <strong>C-Spark Transpiler</strong> automatically <strong>converts C-Spark code into pure C</strong>, making it executable on any standard C compiler.
        </p>
    </div>

    <!-- Example Transpilation Section -->
    <div class="card">
        <h2>Example Transpilation</h2>
        <p>Here’s how C-Spark translates into C:</p>

        <div class="code-block">
            <h3>C-Spark Code</h3>
            <pre>
function main() {
    print("Hello, World!");
}</pre>
        </div>

        <div class="code-block">
            <h3>Transpiled C Code</h3>
            <pre>
#include &lt;stdio.h&gt;

int main() {
    printf("Hello, World!\n");
    return 0;
}</pre>
        </div>
    </div>
</div>
