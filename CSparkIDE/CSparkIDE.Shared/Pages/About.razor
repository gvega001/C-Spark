@page "/about"
@using CSparkIDE.Shared.Services
@inject IFormFactor FormFactor

<PageTitle>About C-Spark IDE</PageTitle>

<style>
    /* Container for the page */
    .about-container {
        max-width: 900px;
        margin: auto;
        padding: 20px;
        font-family: Arial, sans-serif;
    }

    /* Header section */
    .header-container {
        text-align: center;
        padding: 30px 20px;
        margin-bottom: 30px;
        border-bottom: 3px solid #007acc;
        background: linear-gradient(to right, #007acc, #61dafb);
        color: white;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

        .header-container h1 {
            font-size: 2.4rem;
            margin-bottom: 10px;
        }

        .header-container p {
            font-size: 1.2rem;
            margin: 0;
        }

    /* Card-based layout */
    .card {
        background: #ffffff;
        border: 1px solid #ddd;
        border-radius: 10px;
        padding: 20px;
        margin-top: 15px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

        /* Section headers within cards */
        .card h2 {
            font-size: 1.8rem;
            margin-top: 0;
            margin-bottom: 15px;
            color: #007acc;
        }

    /* Code block styling */
    .code-block {
        font-family: Consolas, monospace;
        background: #f5f5f5;
        padding: 15px;
        border-radius: 8px;
        overflow-x: auto;
        border-left: 4px solid #007acc;
        margin-bottom: 15px;
    }

        .code-block h3 {
            font-size: 1.2rem;
            margin-top: 0;
            margin-bottom: 10px;
            color: #333;
        }

    pre {
        margin: 0;
    }
</style>

<div class="about-container">
    <!-- Page Header -->
    <div class="header-container">
        <h1><i class="fas fa-fire"></i> About C-Spark IDE</h1>
        <p>Write C-Spark code and transpile it to C instantly!</p>
    </div>

    <!-- Overview Section -->
    <div class="card">
        <h2><i class="fas fa-cogs"></i> How C-Spark Works</h2>
        <p>
            C-Spark is a <strong>beginner-friendly programming language</strong> that simplifies coding while keeping the <strong>power of C</strong>.
            The <strong>C-Spark Transpiler</strong> automatically <strong>converts C-Spark code into pure C</strong>, making it executable on any standard C compiler.
        </p>
    </div>

    <!-- Example Transpilation Section -->
    <div class="card">
        <h2><i class="fas fa-code"></i> Example Transpilation</h2>
        <p>Here’s how C-Spark translates into C:</p>

        <div class="code-block">
            <h3><i class="fas fa-terminal"></i> C-Spark Code</h3>
            <pre>
function main() {
    print("Hello, World!");
}</pre>
        </div>

        <div class="code-block">
            <h3><i class="fas fa-terminal"></i> Transpiled C Code</h3>
            <pre>
#include &lt;stdio.h&gt;

int main() {
    printf("Hello, World!\n");
    return 0;
}</pre>
        </div>
    </div>
</div>

<!-- FontAwesome Icons -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
